import { Badge } from "../Badge";

export default {
  title: "UI/Badge",
  component: Badge
};

export const Tour = {
  args: { children: "ТУР", variant: "tour" }
};

export const Place = {
  args: { children: "МЕСТО", variant: "place" }
};
